package com.rubbishman.rubbishRedux.experimental.keyBinding.data;

public class BindListener {
    public boolean pressed = false;

    public final Bind bind;

    public BindListener(Bind bind) {
        this.bind = bind;
    }
}
